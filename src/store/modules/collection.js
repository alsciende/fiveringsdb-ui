import Vue from 'vue';
import * as types from '../mutation-types';

// actions
const actions = {};

// getters
const getters = {
  collection: state => state.collection,
};

// mutations
const mutations = {
};

export default {
  state: {
    collection: {},
  },
  getters,
  actions,
  mutations,
};
