/* eslint-disable */
import queryMapper from './queryMapper';

class QueryValidator {
  prune(clauses) {
    return clauses;
  }
  validate(clause) {
    return true;
  }
  // TODO
}

export default new QueryValidator();
